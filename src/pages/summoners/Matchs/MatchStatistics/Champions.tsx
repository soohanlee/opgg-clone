import React from "react";
import styled from "styled-components";

const Champions = () => {
  return <Container>Champions</Container>;
};

export default Champions;

const Container = styled.div`
  border-right: 0.1rem solid ${({ theme }) => theme.colors.silver};
`;
